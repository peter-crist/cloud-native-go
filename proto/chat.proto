syntax = "proto3";
package chat;

service Chat {
    rpc Send(Request) returns (Response) {}
}

message Request {
    string message = 1;
}

message Response {
    string message = 1;
    string sha = 2;
}